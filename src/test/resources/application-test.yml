# Springboot configurations
spring.datasource:
  driver-class-name: org.mariadb.jdbc.Driver

spring.jpa:
  hibernate:
    ddl-auto: none
  show-sql: true
  properties:
    hibernate:
      format_sql: true
